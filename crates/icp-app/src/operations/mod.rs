//! Operations that act on a canister the caller named by principal, or on a
//! ledger, rather than on something a project manifest describes.

pub mod canister_migration;
pub mod snapshot_transfer;
pub mod token;
