canister:
  name: my-canister
  recipe:
    type: motoko
    configuration:
      # entry point file (defaults to main.mo)
      main: src/main.mo
      # additional Motoko compiler flags
      flags: --ai-errors
