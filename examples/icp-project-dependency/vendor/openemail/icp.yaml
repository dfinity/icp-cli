# yaml-language-server: $schema=https://github.com/dfinity/icp-cli/raw/refs/tags/v0.1.0/docs/schemas/icp-yaml-schema.json
#
# `openemail` is a self-contained icp project. It can be developed and deployed
# on its own (`icp deploy` from this directory), and it is also consumed as a
# dependency by the parent project one level up (../../icp.yaml).
#
# In the real workflow this directory would be a git submodule so its source can
# be iterated in place while the parent app is developed.

canisters:
  - name: backend
    build:
      steps:
        - type: pre-built
          path: ../../../icp-pre-built/dist/hello_world.wasm
          sha256: 17a05e36278cd04c7ae6d3d3226c136267b9df7525a0657521405e22ec96be7a

  # `frontend` is an asset canister: it serves the `http_request` endpoint, so
  # `icp deploy` prints a clickable frontend URL for it. As a dependency canister
  # its subdomain is namespaced by the alias — `frontend.openemail.<env>.localhost`
  # — while a compute canister like `backend` (no `http_request`) prints a Candid
  # UI URL instead.
  - name: frontend
    build:
      steps:
        - type: pre-built
          url: https://github.com/dfinity/sdk/raw/refs/tags/0.27.0/src/distributed/assetstorage.wasm.gz
          sha256: 865eb25df5a6d857147e078bb33c727797957247f7af2635846d65c5397b36a6
