canister:
  name: my-canister
  recipe:
    type: "@dfinity/rust"
    configuration:
      # cargo package for canister (required field)
      package: icp-canister
      shrink: true
      metadata:
        - name: "crate:version"
          value: 1.0.0
