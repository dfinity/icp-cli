canister:
  name: icp-template-rust
  build:
    adapter:
      type: script
      commands:
        - cargo build --target wasm32-unknown-unknown --release --locked
        - realpath ./target/wasm32-unknown-unknown/release/icp_template_rust.wasm
